# AWS Configuration
# These are optional - CDK will use AWS CLI defaults if not set
# You can also set them via AWS CLI: aws configure

CDK_DEFAULT_ACCOUNT=your-aws-account-id
CDK_DEFAULT_REGION=eu-central-1

# Cognito Configuration (will be output after CDK deploy)
# These values will be available in CloudFormation outputs
# You don't need to set these manually - they are outputs from the stack
# NEXT_PUBLIC_COGNITO_USER_POOL_ID=your-user-pool-id
# NEXT_PUBLIC_COGNITO_CLIENT_ID=your-client-id
# NEXT_PUBLIC_AWS_REGION=eu-central-1
