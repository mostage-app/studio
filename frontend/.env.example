# Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# AWS Cognito Configuration
# Get these values from AWS CDK stack outputs after deploying the backend
# Run: aws cloudformation describe-stacks --stack-name MostageStudioAuthStack --query 'Stacks[0].Outputs' --output table
NEXT_PUBLIC_COGNITO_USER_POOL_ID=your-user-pool-id-here
NEXT_PUBLIC_COGNITO_CLIENT_ID=your-client-id-here
NEXT_PUBLIC_AWS_REGION=eu-central-1
