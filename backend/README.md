# Backend

This directory is reserved for future backend services.

Currently, authentication is handled directly by the frontend using AWS Cognito. This directory will be used when additional backend services are needed.
